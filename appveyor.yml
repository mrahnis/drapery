build: false

platform: x64

environment:
  matrix:
    - PYTHON_VERSION: 3.6
      PYTHON_ARCH: 64
      MINICONDA: C:\Miniconda36-x64

init:
  - "ECHO %PYTHON_VERSION% %MINICONDA%"

install:
  - "set PATH=%MINICONDA%;%MINICONDA%\\Scripts;%PATH%"
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda
  - conda install conda-build
  - conda info -a
  - conda config --add channels conda-forge
  - "conda create -q -n test-environment python=%PYTHON_VERSION% fiona rasterio shapely click pytest"
  - activate test-environment
  - conda build .
  - conda install drapery --use-local

test_script:
  - pytest

after_test:
  - python ci\appveyor\move-conda-artifacts.py dist

#deploy:
#  release: drapery-v$(appveyor_build_version)
#  description: 'Release description'
#  provider: GitHub
#  auth_token:
#    secure: a546de962484272ddab1f8d2ef8ddebad2b94b70   # your encrypted token from GitHub
#  artifact: dist\*.whl             # upload all wheel packages to release assets
#  draft: false
#  prerelease: false
#  on:
#    branch: master                 # release from master branch only
#    appveyor_repo_tag: true        # deploy on tag push only

artifacts:
  - path: dist\*.tar.bz
